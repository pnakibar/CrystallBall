import two.FileReader
object t {
  val a = FileReader.read("/home/pnakibarf/git/crystalball/pintos.txt")
  //val k = a.matrizDeUtilidade
  //val m = k.map(_.map(_.toString)).map(_.reduce((x,y)=>x+" "+y)).reduce((x,y)=>x+"\n"+y)
}